You are producing the final implementation plan that a consultant 
will review before execution. Your job:

1. ORDER the actions so dependencies execute first (property 
   creations before workflows that use those properties, 
   integrations before actions that depend on them).

2. ASSEMBLE the plan by pulling relevant fields from the prior 
   stages into a coherent whole.

3. POLISH the approach_summary and rationale into consultant-grade 
   prose. The consultant reading this should feel confident that 
   a senior expert made the decision.

4. FORMAT action descriptions for the UI. The UI only renders 
   action descriptions to humans, so they must be brief and 
   business-focused.

Inputs (substituted below):
{{requirement}}
{{architecture_decision}}
## GAP DETECTION REPORT
```json
{{stage_5_gaps}}
```

Rules for ordering:
- The `depends_on` list MUST ONLY contain `action_id`s that belong to other actions within THIS specific plan's `actions` list. NEVER use external requirement IDs (like REQ-002) as dependencies.
- If action B references a property/entity that action A creates (and action A is in this plan), then B depends_on A.
- Multiple property creations can run in any order relative to each other (empty depends_on).
- CRITICAL BUNDLING: If the workflow or main operation depends on properties that were flagged as missing, you MUST generate property creation actions (e.g., `hubspot.create_custom_property`) and place them BEFORE the workflow action in the list. Do not omit the main action; just bundle its prerequisites first.
- The `action.operation_id` must match valid operations (e.g. `hubspot.create_custom_property`, `hubspot.create_workflow`).

Rules for action descriptions:
- Each action.description MUST be plain business language, max 12 words.
- GOOD: "Set customer segment to At-Risk"
- BAD: "Execute hubspot.create_workflow with parameters { ... }"
- The JSON parameters stay in action.parameters for the executor.

Rules for polish:
- approach_summary: One sentence, max 25 words, active voice, specific business language.
- rationale: 3-5 sentences that reference specific customer 
  requirements, capability decision criteria, and any relevant 
  gotchas

Return an ImplementationPlan with all fields populated.